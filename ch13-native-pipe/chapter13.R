# 第13章　ネイティブパイプ |>
#
# 『Rによるデータ分析 入門から実践』の第13章のサンプルコードです。
# リポジトリのルートを作業ディレクトリにして、上から順に実行してください。

# ── block 01 ────────────────────────────────────────
library(tidyverse)

# ── block 02 ────────────────────────────────────────
uriage <- c(100, 200, 300, 250)
round(mean(uriage), 1)

# ── block 03 ────────────────────────────────────────
uriage |> mean() |> round(1)

# ── block 04 ────────────────────────────────────────
c(3, 1, 2) |> sort()

# ── block 05 ────────────────────────────────────────
library(magrittr)
c(3, 1, 2) %>% sort()

# ── block 06 ────────────────────────────────────────
quote(x |> f())

# ── block 07 ────────────────────────────────────────
quote(x %>% f())

# ── block 08 ────────────────────────────────────────
quote(x %>% f())[[1]]

# ── block 09 ────────────────────────────────────────
sonomama <- function(x) x
c(native = system.time(for (i in 1:1e5) 1 |> sonomama())[["elapsed"]],
  magrittr = system.time(for (i in 1:1e5) 1 %>% sonomama())[["elapsed"]])

# ── block 10 ────────────────────────────────────────
c(1, 2, 3) %>% paste("x", .)

# ── block 12 ────────────────────────────────────────
uriage_df <- data.frame(a = c(1, 2, 3), b = c(2, 4, 7))
uriage_df |> lm(b ~ a, data = _) |> coef()

# ── block 15 ────────────────────────────────────────
uriage_df |> lm(b ~ a, data = _) |> _$coefficients

# ── block 16 ────────────────────────────────────────
uriage_df |> lm(b ~ a, data = _) |> _$coefficients[[2]]

# ── block 17 ────────────────────────────────────────
model <- lm(b ~ a, data = uriage_df)
coef(model)[[2]]

# ── block 19 ────────────────────────────────────────
1:3 %>% sum

# ── block 20 ────────────────────────────────────────
lab <- function(x) deparse(substitute(x))
uriage_df |> lab()

# ── block 21 ────────────────────────────────────────
uriage_df %>% lab()

# ── block 22 ────────────────────────────────────────
uriage_df$b |> hist()

# ── block 23 ────────────────────────────────────────
uriage_df |> filter(b > 3)

# ── block 24 ────────────────────────────────────────
uriage_df %>% filter(b > 3)

# ── block 26 ────────────────────────────────────────
-1:3 |> sum()

# ── block 27 ────────────────────────────────────────
-1 |> abs()

# ── block 28 ────────────────────────────────────────
c(1, 2, 3) %>% paste(., "個")

# ── block 29 ────────────────────────────────────────
c(4, 9) %>% paste(sqrt(.))

# ── block 30 ────────────────────────────────────────
kazu <- function(...) length(list(...))
c(1, 2) %>% kazu(.)

# ── block 31 ────────────────────────────────────────
c(4, 9) %>% kazu(sqrt(.))

# ── block 32 ────────────────────────────────────────
c(1, 2) %>% kazu("a", .)

# ── block 33 ────────────────────────────────────────
c(4, 9) %>% { paste(sqrt(.)) }

# ── block 34 ────────────────────────────────────────
tenpo_betsu <- uriage_df |> filter(b > 3)
tenpo_betsu |> nrow()

