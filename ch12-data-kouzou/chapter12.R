# 第12章　データ構造の正体
#
# 『Rによるデータ分析 入門から実践』の第12章のサンプルコードです。
# リポジトリのルートを作業ディレクトリにして、上から順に実行してください。
#
# ※ 本文には、わざと失敗させてエラーメッセージを読むコードも載せています。
#    それらはこのファイルには含めていません（上から実行して止まらないようにするため）。
#    エラー例は本文を参照してください。

# ── block 01 ────────────────────────────────────────
library(tidyverse)

# ── block 02 ────────────────────────────────────────
x <- 42
length(x)

# ── block 03 ────────────────────────────────────────
x[1]

# ── block 04 ────────────────────────────────────────
1:30

# ── block 05 ────────────────────────────────────────
c(typeof(1), typeof(1L), typeof("a"), typeof(TRUE))

# ── block 06 ────────────────────────────────────────
c(class(1), class(1L))

# ── block 07 ────────────────────────────────────────
c(typeof(c(TRUE, 1L)), typeof(c(1L, 1.5)), typeof(c(1.5, "a")))

# ── block 08 ────────────────────────────────────────
sum(c(TRUE, FALSE, TRUE, TRUE))

# ── block 09 ────────────────────────────────────────
c(1, 2, 3, 4) + c(10, 20)

# ── block 10 ────────────────────────────────────────
c(1, 2, 3) + c(10, 20)

# ── block 11 ────────────────────────────────────────
l <- list(a = 1, b = "x", c = c(1, 2, 3))
str(l)

# ── block 12 ────────────────────────────────────────
c(class(l[1]), class(l[[1]]))

# ── block 13 ────────────────────────────────────────
l[1]

# ── block 14 ────────────────────────────────────────
l[[1]]

# ── block 15 ────────────────────────────────────────
l[c(1, 2)]

# ── block 16 ────────────────────────────────────────
l[["c"]] + 1

# ── block 17 ────────────────────────────────────────
d <- tibble(x = 1:3, y = c("a", "b", "c"))
c(typeof(d), class(d)[1])

# ── block 18 ────────────────────────────────────────
length(d)

# ── block 19 ────────────────────────────────────────
c(nrow(d), ncol(d))

# ── block 20 ────────────────────────────────────────
c(class(d["x"])[1], class(d[["x"]]))

# ── block 21 ────────────────────────────────────────
d1 <- data.frame(value = 1:3)
d1$val

# ── block 22 ────────────────────────────────────────
t1 <- tibble(value = 1:3)
t1$val

# ── block 23 ────────────────────────────────────────
d2 <- data.frame(x = 1:6)
d2$y <- c(1, 2)
d2

# ── block 25 ────────────────────────────────────────
d3 <- data.frame(x = 1:3, y = 4:6)
class(d3[, "x"])

# ── block 26 ────────────────────────────────────────
class(tibble(x = 1:3, y = 4:6)[, "x"])[1]

# ── block 27 ────────────────────────────────────────
class(d3[, "x", drop = FALSE])

# ── block 28 ────────────────────────────────────────
a <- c(1, 2, 3)
b <- a
b[1] <- 99
a

# ── block 29 ────────────────────────────────────────
b

# ── block 30 ────────────────────────────────────────
zero_x <- function(df) { df$x <- 0; df }
d4 <- tibble(x = 1:3)
zero_x(d4)
d4

# ── block 31 ────────────────────────────────────────
f <- factor(c("2026", "2025", "2026"))
c(typeof(f), class(f))

# ── block 32 ────────────────────────────────────────
attributes(f)

# ── block 33 ────────────────────────────────────────
f2 <- factor(c("A", "B"))
f2[3] <- "C"

# ── block 34 ────────────────────────────────────────
f2

# ── block 35 ────────────────────────────────────────
s <- c("新宿店", "大阪店", "オンライン")
sort(s)

# ── block 36 ────────────────────────────────────────
tibble(店舗 = s, 売上 = c(300, 200, 100)) |>
  group_by(店舗) |>
  summarise(合計 = sum(売上))

# ── block 37 ────────────────────────────────────────
f4 <- factor(s, levels = c("新宿店", "大阪店", "オンライン"))
levels(f4)

# ── block 38 ────────────────────────────────────────
tibble(店舗 = f4, 売上 = c(300, 200, 100)) |>
  group_by(店舗) |>
  summarise(合計 = sum(売上))

# ── block 39 ────────────────────────────────────────
s2 <- c("大阪店", "オンライン", "新宿店")
levels(fct_inorder(s2))

# ── block 40 ────────────────────────────────────────
f3 <- factor(c("A", "B"), levels = c("A", "B", "C"))
table(f3)

# ── block 41 ────────────────────────────────────────
droplevels(f3)

# ── block 42 ────────────────────────────────────────
m <- matrix(1:6, nrow = 2)
m

# ── block 43 ────────────────────────────────────────
attributes(m)

# ── block 44 ────────────────────────────────────────
d5 <- data.frame(x = 1:3, y = c("a", "b", "c"))
apply(d5, 1, function(r) r["x"])

# ── block 45 ────────────────────────────────────────
d5 |> mutate(z = x * 2)

# ── block 46 ────────────────────────────────────────
e <- character(0)
length(e)

# ── block 47 ────────────────────────────────────────
1:length(e)

# ── block 48 ────────────────────────────────────────
seq_along(e)

# ── block 49 ────────────────────────────────────────
t3 <- tibble(g = c("a", "b"), d = list(1:3, 1:5))
t3

# ── block 50 ────────────────────────────────────────
t3 |> mutate(n = map_int(d, length))

# ── block 51 ────────────────────────────────────────
d6 <- data.frame(value = 1:3)
as_tibble(d6)$val

# ── block 52 ────────────────────────────────────────
col <- "x"
d7 <- tibble(x = 1:3)
d7[[col]]

