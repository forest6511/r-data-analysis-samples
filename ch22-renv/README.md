# 第22章　再現性（renv / プロジェクト構成）

『Rによるデータ分析 入門から実践』の第22章です。

この章で扱うのは、R コンソールで実行するコードではなく **`.qmd` ファイルの中身とコマンドライン操作**です。
そのため、まとめて実行できる `chapterNN.R` は用意していません。本文の手順どおりに、手元で作成・実行してください。

## この章で使うデータ

- [`uriage_2y.csv`](../uriage_2y.csv)

## 章の構成

- **22.1** なぜ動かなくなるのか
- **22.2** renv とは何をするものか
- **22.3** 導入する — renv::init()
- **22.4** `renv.lock` の中身
- **22.5** 使うパッケージが増えたとき
- **22.6** 元に戻す — renv::restore()
- **22.7** 3つのコマンドの関係
- **22.8** 何を配って何を配らないか
- **22.9** フォルダの構成
- **22.10** 🔴 `setwd()` を書かない
- **22.11** 乱数を使うときは種を固定する
- **22.12** どこまでやるか
- **22.13** まとめ

[← 章の一覧に戻る](../README.md)
