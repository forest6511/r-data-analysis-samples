# 第15章　反復処理 purrr/map

『Rによるデータ分析 入門から実践』の第15章のサンプルコードです。

## 章の構成

- **15.1** `for` で書いた繰り返しの問題
- **15.2** `map()` の形
- **15.3** `sapply()` が返す型は中身で変わる
- **15.4** 型を指定する `map_*()`
- **15.5** `map_dbl()` は日付を数値にする
- **15.6** 2つの対象を並べて回す `map2()`
- **15.7** 名前も一緒に回す `imap()`
- **15.8** 結果を表にまとめる `list_rbind()`
- **15.9** 複数ファイルを1つの表にする
- **15.10** 行ごとの処理
- **15.11** 結果が要らない繰り返し `walk()`
- **15.12** 途中で失敗しても止めない `possibly()`
- **15.13** 絞り込みと畳み込み
- **15.14** 入れ子の繰り返し
- **15.15** どれを使うか
- **15.16** まとめ

## 実行のしかた

リポジトリのルートを作業ディレクトリにして実行してください（RStudio の Session → Set Working Directory → Choose Directory...）。

```r
source("ch15-purrr/chapter15.R")
```

1行ずつ試したいときは、[`chapter15.R`](./chapter15.R) を開いて上から順に実行してください。

[← 章の一覧に戻る](../README.md)
